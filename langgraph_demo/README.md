# LangGraph Demo

간단한 상태 그래프 예제입니다. 입력된 이름을 받아 인사말을 만드는 두 개의 노드를 실행합니다.

## 실행

```powershell
cd langgraph_demo
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```

예상 결과:

```text
안녕하세요, LangGraph!
```
